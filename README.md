# Profit-Prediction
A linear Regression model in a form of a web application that takes user input for the independent variables and predicts a single profit which the start up is possible to get if that investment is made.
